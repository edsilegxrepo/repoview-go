<?xml version="1.0" ?>
<rss version="2.0">
<channel>
  <title>{{ .Repo.Title }}</title>
  <link>{{ if .URL }}{{ .URL }}/repoview/latest-feed.xml{{ else }}latest-feed.xml{{ end }}</link>
  <description>Latest packages for {{ .Repo.Title }}</description>
  <lastBuildDate>{{ now | rssTime }}</lastBuildDate>
  <generator>RepoView-Go</generator>
  {{ range .Latest }}
  <item>
    <guid>{{ if $.URL }}{{ $.URL }}/repoview/{{ end }}{{ .Filename }}+{{ if .Epoch }}{{ .Epoch }}{{ else }}0{{ end }}:{{ .VersionRelease }}.{{ .Arch }}</guid>
    <link>{{ if $.URL }}{{ $.URL }}/repoview/{{ end }}{{ .Filename }}</link>
    <pubDate>{{ .TimeBuild | rssTime }}</pubDate>
    <title>Update: {{ .Name }}-{{ .VersionRelease }}</title>
    <description><![CDATA[
      <p>
        <strong>Package:</strong> {{ .Name }}<br/>
        <strong>Summary:</strong> {{ .Summary }}
      </p>
      <p>
        <strong>Description:</strong><br/>
        {{ .Description }}
      </p>
      <h3>Changes:</h3>
      <table border="0" cellpadding="0" cellspacing="5">
      {{ range .AllVersions }}
        <tr>
          <td valign="top"><a href="{{ if $.URL }}{{ $.URL }}/{{ else }}../{{ end }}{{ .LocationHref }}">{{ .Name }}-{{ .VersionRelease }}.{{ .Arch }}</a>
            [<span style="white-space: nowrap">{{ .SizePackage | humanSize }}</span>]</td>
          {{ if .Changelog }}
          <td valign="top">
            <strong>Changelog</strong>
            by <span>{{ .Changelog.Author }} ({{ .Changelog.Date | ymd }})</span>:
            <pre style="margin: 0pt 0pt 5pt 5pt">{{ .Changelog.Changelog }}</pre>
          </td>
          {{ else }}
          <td valign="top">
            <em>(no changelog entry)</em>
          </td>
          {{ end }}
        </tr>
      {{ end }}
      </table>
    ]]></description>
  </item>
  {{ end }}
</channel>
</rss>
